name: CI-Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # ---------------------------
      # Checkout Repo
      # ---------------------------
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: echo "✅ Repository checked out successfully."

      # ---------------------------
      # Python Setup
      # ---------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: |
          echo "🐍 Python 3.10 environment setup complete."
          python --version
          pip --version

      # ---------------------------
      # Dependency Installation
      # ---------------------------
      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          echo "✅ Dependencies installed successfully."

      # ---------------------------
      # GCP Authentication
      # ---------------------------
      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
      - run: echo "🔑 GCP authentication completed successfully."

      - name: Set up gcloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: mlops-474118

      - name: Verify GCP Auth
        run: |
          gcloud auth list
          gsutil ls gs://mlops-474118-artifacts
      # ---------------------------
      # DVC Pull
      # ---------------------------
      - name: DVC pull
        run: |
          echo "🚀 Starting DVC setup..."
          pip install dvc[gs]
          echo "✅ DVC and gs extension installed."

          if [ -d ".dvc" ]; then
            echo "📂 Found .dvc in repo root."
            echo "🔄 Pulling data from remote..."
            dvc pull -r gcsremote || { echo "❌ DVC pull failed in repo root."; exit 1; }
            echo "✅ DVC pull completed in repo root."
          elif [ -d "mlops/.dvc" ]; then
            echo "📂 Found .dvc inside mlops/ folder."
            cd mlops
            echo "🔄 Pulling data from remote (mlops)..."
            dvc pull -r gcsremote || { echo "❌ DVC pull failed in mlops/"; exit 1; }
            echo "✅ DVC pull completed in mlops/."
          else
            echo "⚠️ No DVC repository found — skipping DVC pull step."
          fi

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
      - run: echo "🔑 GCP authentication completed successfully."

      - name: Decode GCP key
        run: |
          echo "${{ secrets.GCP_SA_KEY_B64 }}" | base64 --decode > /tmp/gcp-key.json
          
      - name: Setup Feast
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="/tmp/gcp-key.json"
          
          echo "🔍 Verifying GCP access..."
          gcloud auth activate-service-account --key-file=$GOOGLE_APPLICATION_CREDENTIALS
          gsutil ls gs://mlops-474118-artifacts/data/

          echo "🚀 Applying Feast definitions..."
          cd feast_repo
          feast apply
          feast materialize-incremental $(date +%Y-%m-%d)

      - name: Verify GCS Access
        run: |
          echo "${{ secrets.GCP_SA_KEY }}" > /tmp/gcp-key.json
          export GOOGLE_APPLICATION_CREDENTIALS="/tmp/gcp-key.json"
          gcloud auth list
          gsutil ls gs://mlops-474118-artifacts/data/


      - name: Start MLflow tracking server
        run: |
          nohup mlflow server \
            --backend-store-uri sqlite:///mlflow.db \
            --default-artifact-root ./mlruns \
            --host 127.0.0.1 \
            --port 5000 &
          sleep 25
          
      - name: Train model and compute accuracy
        run: |
          python scripts/train.py
          echo "## ✅ Model Report" > report.md
          echo "" >> report.md
          echo "### 📊 Metrics" >> report.md
          cat metrics.txt >> report.md

      - name: Run Inference
        run: |
          gsutil -m cp -r gs://mlops-474118-artifacts/models/best_model_20251108T121848Z/ ./best_model
          python scripts/inference.py --eval_csv "gs://mlops-474118-artifacts/data/processed/eval.csv" --local_model_dir ./best_model

      - name: Run pytest
        run: |
          echo "🧪 Running unit tests with pytest..."
          pytest -q || { echo "❌ Tests failed"; exit 1; }
          echo "✅ All tests passed successfully."

      - name: 🧰 Set up CML
        run: |
          npm install -g @dvcorg/cml

      - name: 📝 Post training results as comment
        env:
          REPO_TOKEN: ${{ secrets.HUB_TOKEN }}
        run: |
          cml comment create report.md


